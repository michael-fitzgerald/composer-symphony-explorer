import { SymphonyByAssetPipe } from './symphony-by-asset.pipe';

describe('SymphonyByAssetPipe', () => {
  it('create an instance', () => {
    const pipe = new SymphonyByAssetPipe();
    expect(pipe).toBeTruthy();
  });
});
